package config

import "time"

const (
	PRICE_REFRESH_INTERVAL        = 1 * time.Second
	CHART_REFRESH_INTERVAL        = 1 * time.Minute
	ROUND_REFRESH_INTERVAL        = 1 * time.Second
	LEADER_BOARD_REFRESH_INTERVAL = 1 * time.Minute
)
